# Represents how many rows and columns the board will have (can be adjusted)
GRID_SIDE_LENGTH: int = 3
ROW_AMOUNT: int = GRID_SIDE_LENGTH
COLUMN_AMOUNT: int = GRID_SIDE_LENGTH

# Determines how many marks are needed in a line to win
WIN_CONDITION: int = GRID_SIDE_LENGTH

# Holds the turn number
turnNumber: int = 1